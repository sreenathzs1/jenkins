pipeline {
    agent {
        label "ANSIBLE"
    }

    stages {

        stage('Find the server') {
            steps {
                sh 'echo'
            }
        }

        // ENV nedd to rpelaced with appropriate, Need to find how

        stage('Deploy to DEV Env') {
            when { ... }
            steps {
                sh 'ansible-playbook ....'
            }
        }

        stage('Deploy to PROD ENV') {
            when { .... }
            steps {
                sh 'ansible-playbook ....'
            }

        }
    }
}