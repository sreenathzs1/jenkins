pipeline {
    agent any

    enivironment {
        PROJECT_NAME = "ROBOSHOP"
    }
    
    stages{

        stage('ONE') {
            steps{
                .sh "echo &{PROJECT_NAME}"
            }
        }
        stage('two'){

            enivironment {
                PROJECT_NAME = "TODO"
            }
            steps {
                .sh "echo ${PROJECT_NAME}"
            }
        }
    }

}